# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/real_estate_erp?schema=public"

# Direct database URL for migrations (Neon: use connection string WITHOUT -pooler)
# For local development, can be the same as DATABASE_URL
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/real_estate_erp?schema=public"

# JWT Secret (change this in production!)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Node Environment
NODE_ENV="development"
